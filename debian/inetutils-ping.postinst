#!/bin/sh
set -e

if [ "$1" = 'configure' ]; then
  for file in ping ping6; do
    if [ -e /usr/bin/$file ] && [ ! -e /bin/$file ]; then
      ln -s /usr/bin/$file /bin/$file
    fi
  done
fi

#DEBHELPER#
